nayuta_data_test_sqlite_profile:
  target: dev
  outputs:
    dev:
      type: sqlite
      threads: 1
      database: "database"
      schema: 'main'
      schemas_and_paths:
        main: 'sqlite/etl.db'
        raw: 'sqlite/raw.db'
      schema_directory: 'sqlite'